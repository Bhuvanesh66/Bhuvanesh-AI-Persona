# --- Core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- LLM + Embeddings (GitHub Models — OpenAI-compatible, uses GITHUB_TOKEN) ---
openai>=1.59.6

# --- Vector store (Supabase / Postgres + pgvector) ---
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- Ingestion ---
requests==2.32.3          # GitHub REST API
pypdf==5.1.0              # resume PDF parsing
